# Seedance 2.0 提示词指南

适用于所有 Seedance 2.0 系列模型（`dreamina-seedance-2-0-260128` 标准 / `-fast-260128` 快速）。Seedance 2.0 原生支持音视频联合生成、深度多模态参考与视频编辑。

> Skill 内 reference 文档 —— 当 agent 需要写更高质量的 video prompt 时按需翻阅。已剔除原版指南中的视觉素材 URL，仅保留可文本复用的公式与示例。

---

## 1. 文本指令公式

### 1.1 三要素

| 要素 | 必需 | 作用 |
|------|------|------|
| **主体** | ✅ | 谁正在做什么动作 — 指令的逻辑基石 |
| **环境** | ❌ | 空间背景、光影、视觉风格 |
| **运镜 / 音频** | ❌ | 镜头调度、音效、配音、字幕 |

> 模型遵循自然语言逻辑，不要堆砌关键词；用一句通顺的中文描述即可。

### 1.2 多模态指代

参考素材按上传顺序编号，提示词里用 ``图片1``、``图片2``、``视频1`` 等**精确指代**：

> *"画面参考`图片1`的构图"*
> *"动作参考`视频2`"*
> *"参考`图片1`、`图片2`、`图片3`中的女子形象"*

模型会自动提取参考对象的核心特征，再结合文本描述生成。

---

## 2. 文字生成（T2V / I2V / R2V / V2V 均支持）

模型自动匹配文字风格与颜色。如需精确控制，可指定颜色、风格、出现方式、出现时机、出现位置。

**字符规范：** 常用字优先，避免生僻字与特殊符号。

### 2.1 Slogan / 广告语

```
「文字内容」+「出现时机」+「出现位置」+「出现方式」，「文字特征（颜色、风格）」
```

示例：

> 手绘漫画风格，三个人围坐在一起吃`图片1`中的炸鸡，气氛友好愉悦，后画面逐渐模糊，画面中部显示文字"快乐尽在 Seedance"。

> 对文字呈现要求严格时，参考 §3.2 多图参考 → Logo 参考的方式。

### 2.2 字幕

```
画面底部出现字幕，字幕内容为"…"，字幕需与音频节奏完全同步。
```

示例：

> 生成带有画外音的视频。一个深沉、平静的男声说："在宏大的宇宙中，我们的世界不过是一个短暂的瞬间。然而，在其中，生命不顾一切地繁荣。"场景应从夜晚缓慢过渡到黎明，星星逐渐消失，太阳从山后升起。画面底部按照台词出现字幕。

> 图片中的两人在办公室聊天，女性先说话，她说道："你每次卡点到，是不是很享受这种刚刚好的感觉？"男性笑着回应："我有我的节奏"。角色说话时，对话随意自然，画面底部出现对应台词字幕。

### 2.3 气泡台词

```
「角色」说："…"，角色说话时周围出现气泡，气泡里写着台词。
```

示例：

> `图片1`中的两人穿着运动服在学校的操场跑步，女孩看向男孩，自信地笑着说："We can definitely do it!"镜头切到男孩的近景，他犹豫地回答："Are you sure?"镜头切回女孩的中近景，她语气轻快地说："Yes!"情绪明亮而坚定。说话的角色周边出现气泡，气泡里是对应台词。

---

## 3. 图片参考

支持主体多视角参考与多图参考（场景图、分镜图等）。多张图按顺序上传，提示词用 `图片1`～`图片n` 指代。

### 3.1 主体多视角

```
参考/提取/结合 +「图片n」中的「主体」，生成「画面描述」，保持「主体」特征一致。
```

示例：

商品 / 3C 数码：
> 提取`图片1`、`图片2`、`图片3`的相机，把背景换成白色，相机在一个白色桌子上，镜头以特写的形式聚焦相机，然后以相机为主体缓慢旋转，清晰展示相机的正面侧面以及背面。

商品 / 家居：
> 背景为暖调居家场景，中景呈现参考图中的保温杯，镜头平稳推近至保温杯近景，镜头外一只手自然入镜轻握杯身拿起保温杯，镜头跟拍手部微微旋转动作展示。

角色：
> 参考`图片1`、`图片2`、`图片3`中的女子形象，生成她在一家咖啡店吃蛋糕的画面。

### 3.2 多图参考

```
参考/提取/结合/按照/生成 +「图片n」中的「被参考元素描述」，生成「画面描述」，保持「被参考元素」特征一致。
```

#### Logo 参考

> 背景是霓虹闪烁的未来都市空中廊道，飞行器与全息广告交织，参考`图片2`中的女孩，先用中景展示女孩放飞带有全息投影的银色悬浮灯，再镜头拉远展现漫天悬浮灯，画面逐渐模糊，后出现`图片1`的 Logo，整体风格为 3D 赛博朋克科幻动画风格。

#### 多主体参考

> 参考图片中的猫猫和狗狗，在一个温馨的公寓里，狗狗在趴着吃狗粮，猫猫走过来，伸出爪子碰了碰狗狗，狗狗看到猫猫后停下吃饭，猫猫依偎在狗狗身边。画面采用暖色调。

#### 多元素参考（角色 + 服装 + 场景 + 标识）

> 场景设定在`图片4`中的餐厅内，店内人来人往。`图片1`里的女孩身着`图片2`中的服装，正在整理柜台上的物品。`图片3`中的男孩是一位顾客，他走上前，想要向女孩索要联系方式。`图片5`中的标识始终显示在画面的右下角。

#### 分镜图参考

多宫格分镜：
> 参考图片中的分镜图，生成打斗激烈的打斗场面。图片中的各个分镜构图要按照顺序出现，之后二人激烈打斗。

按分镜组合多图：
> 参考`图片3`中的分镜构图，女孩正在等爸爸做好饭，她说："아빠, 배고파요!"，女孩形象参考`图片1`。接着镜头向右横摇，切换至`图片4`的画面和构图，爸爸形象参考`图片2`，爸爸回答她："거의 다 됐어"。

---

## 4. 视频参考

视频按顺序上传，用 `视频1`～`视频n` 指代。

### 4.1 动作参考

```
参考「视频n」的「动作描述」，生成「画面描述」，保持动作细节一致。
```

示例：

影视：
> 参考`视频1`的人物动作和镜头语言，生成`图片2`和`图片1`的打斗场面，`图片2`是左边人物，`图片1`是右边人物。有激烈的背景音乐。

营销：
> 参考`视频1`中马的奔跑形态，生成一匹金色的骏马在草原上奔跑，随即定格其奔跑的华丽姿态，变成一个马形的金吊坠。

### 4.2 运镜参考

```
参考「视频n」的「运镜描述」，生成「画面描述」，保持运镜一致。
```

示例：

> 参考`视频1`的运镜，做一个科技园区的概念视频，以`图片1`中的高楼为视觉中心，同为第一视角俯冲，体现出`图片1`中园区的科技感。

### 4.3 特效参考

```
参考「视频n」的「特效描述」，生成「画面描述」，保持特效一致。
```

示例：

> 参考`视频1`的金色粒子特效，让`图片2`中的人物吹笛子的同时，身边环绕一样的粒子特效。

> 参考`视频1`的特效，让`图片1`中的女生长出相同的翅膀，翅膀生成轨迹一致。

---

## 5. 视频编辑

### 5.1 元素增 / 删 / 改

```
增加：在「视频n」的「时间位置」+「空间位置」，增加「理想元素描述」。
删除：删除「视频n」中的「被删除元素」，视频其他内容保持不变。
修改：将「视频n」中的「被更换元素描述」，替换为「理想元素描述」。
```

示例：

增：
> 在`视频1`的台面上添加炸鸡、披萨等小吃。

删：
> 清除`视频1`桌面上的其他零件和工具，保持桌面整洁干净，桌面上只有他俩手里的。

改：
> 将`视频1`中的香水替换成`图片1`中的面霜，动作和运镜不变。

### 5.2 视频延长

```
向前/向后延长「视频n」+「需延长的视频描述」
生成「视频n」之前/之后的内容 +「需延长的视频描述」
```

> 模型自动截取衔接部分进行合成，输入视频原有片段不会重复生成。

示例：

向后延长：
> 生成`视频1`之后的内容，迟到的两个男士跑向他们，五个人终于见面，友好聊天。

向前延长：
> 向前延长`视频1`，给白衣男子过肩镜头，白衣男子说："It's not that bad. You're just stressed."

### 5.3 轨道补齐

```
「视频1」+「过渡画面描述」+ 接「视频2」+「过渡画面描述」+ 接「视频3」
```

**限制：** 最多 3 段视频输入，总时长 ≤ 15 秒。模型自动截取首尾衔接部分。

示例：

> `视频1`，树叶落地的瞬间，激起金色粒子特效，一阵风吹过，接`视频2`。

---

## 6. 速查清单

写 prompt 前先过一遍：

1. **主体明确？** — 谁、做什么动作
2. **多素材按顺序上传** — 提示词用 `图片n`/`视频n` 精确指代
3. **文字内容用常用字** — 避免生僻字、特殊符号
4. **环境/运镜/音频是加分项** — 必要时再加，过载反而干扰
5. **视频编辑限制** — 轨道补齐 ≤ 3 段、总时长 ≤ 15s
6. **审核敏感点** — 默认 `generate_audio: true` 时音频也会被审核；中性场景如音频生成被拒，可关 `--no-generate-audio` 重试

## 7. 常见任务 → 模板速选表

| 任务 | 公式 | 章节 |
|------|------|------|
| 文生视频（最简） | 主体 + 动作 [+ 环境 + 运镜] | §1.1 |
| 视频里加广告语 | 描述 + "画面中部显示文字'…'" | §2.1 |
| 视频里加字幕 | 描述 + "画面底部出现字幕，字幕内容为'…'，与音频同步" | §2.2 |
| 角色对白 | 角色说："…"，画面底部出现台词字幕 | §2.2 |
| 角色对白（漫画感） | 角色说："…"，周围出现气泡，气泡里写着台词 | §2.3 |
| 一图生视频 | "参考`图片1`中的<主体>，生成<画面描述>" | §3.1 |
| 多图组合 | "参考`图片1`<X>，`图片2`<Y>，生成<画面>" | §3.2 |
| 模仿动作 | "参考`视频1`的<动作>，生成<画面>" | §4.1 |
| 模仿运镜 | "参考`视频1`的<运镜>，生成<画面>" | §4.2 |
| 模仿特效 | "参考`视频1`的<特效>，生成<画面>" | §4.3 |
| 视频里加东西 | "在`视频1`的<位置>，增加<元素>" | §5.1 |
| 视频里去东西 | "删除`视频1`中的<元素>，其他不变" | §5.1 |
| 视频里换东西 | "将`视频1`中的<原>替换为<新>，动作运镜不变" | §5.1 |
| 续写视频 | "生成`视频1`之后的内容，<新增情节>" | §5.2 |
| 拼接视频 | "`视频1` + 过渡描述 + 接`视频2`" | §5.3 |
